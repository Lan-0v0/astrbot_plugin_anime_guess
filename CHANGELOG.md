# 更新日志

本项目的版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## v0.0.3 - 2026-08-31

新增「问 xxx」显式提问指令，和「猜 xxx」一样无需 @ 机器人。

### 新增

- `问 <你的提问>`：对谜底发起提问，bot 回答 是／否／不清楚。比如
  `问 这个角色会使用魔法吗？` → `是`。分隔符和「猜」保持一致，空格、全角空格、
  冒号都认。
- 这是**加法**，不影响原有玩法：@bot 提问、不带指令的疑问句照旧可用。
- 「问 xxx」不受「仅在被@时回答提问」限制，也不要求带疑问标记——玩家写了「问」
  就是在问谜底，所以 `问 男主` 这种也照样受理。
- 裁判出错时「问 xxx」会明确回一句提示，而不是悄无声息（隐式疑问句失败仍保持沉默，
  免得把群里的普通聊天变成一串报错）。
- `/ag` 帮助文案加入「问」这一行。

### 其他

- 要求「问」后面必须有分隔符，`问题是什么`、`问号` 这类普通消息不会被当成指令。
- 新增 17 个单元测试（合计 207 个）与 16 项逐功能验收检查（合计 80 项）。

## v0.0.2 - 2026-08-31

修复对局中提问被误当成开局请求的问题。

### 修复

- 提问识别漏判导致答非所问。玩家问「是2020年之前的番还是之后的番？是之前的回答是，
  不是之前的回答否」时，bot 回的是「本群已经有一局在进行了，先"/ag 结束"再开新的吧~」。

  原因有两层：一是问号只在句尾才算疑问标记，而这句的问号在句中，且「A还是B」这种
  选择问句没有对应标记，于是 `looks_like_question` 判成非提问、监听器不作声；二是没有
  任何发送动作、消息又是 @bot 的，于是继续走到 LLM 主链路，被函数工具当成开局请求，
  把开局拦截文案发了出来。

  现在：问号出现在任何位置都算提问，并补上「还是 / 或者 / 是不 / 有无」等选择问句标记。
- 补一层兜底：对局进行中若开局工具仍被误触发，看着像提问就交给裁判回答，
  而不是发「已经有一局在进行了」；确实是重复开局时只回工具结果、不再发送误导文案。

### 其他

- 新增 11 个回归测试（合计 190 个），覆盖上述原句、句中问号与选择问句，
  同时把「结束游戏」「再来一局」等自然语言控制指令钉成非提问，保证它们仍能走到函数工具。

## v0.0.1 - 2026-08-31

首个发布版本。

### 新增

- `/ag` 指令族：`作品`／`角色`／`随机` 开局，`结束` 揭晓谜底，`排行榜` 查看战绩，
  不带参数显示帮助。别名 `/animeguess`、`/动漫猜谜`。
- 多人抢答：开局后监听本群所有消息，任何人都能用 `猜 xxx` 抢答，猜对算抢答者的战绩，
  而不是开局者的。
- LLM 裁判：依据谜底档案回答玩家提问（只输出「是／否／不清楚」），并判断抢答是否命中，
  支持中日文互指、简繁差异、别名昵称与小笔误。别名精确命中时走本地匹配，不消耗模型调用。
- 三个数据来源库：Bangumi（默认）、AniList、萌娘百科，均优先取中文名、缺失时回退原名。
- 排行榜持久化，固定显示前五名，空缺名次显示「虚以待位」。
- 自然语言开局／结束：通过 AstrBot 函数工具实现，可在配置里关闭。
- 「仅在被@时回答提问」开关，供活跃群减少模型调用；抢答与私聊不受此限制。
